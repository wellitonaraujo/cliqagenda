const OfficeIcon = ({ color = '#000', size = 24 }: { color?: string; size?: number }) => (
    <svg width={size} height={size} fill={color} viewBox="...">...</svg>
  );
  export default OfficeIcon;
  